name: pomo
description: A Pomodoro Timer app with task management using Eisenhower Matrix

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Если хотите использовать flutter_gif, добавьте так:
  # flutter_gif: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Если будете использовать локальные GIF, добавьте:
  # assets:
  #   - assets/images/